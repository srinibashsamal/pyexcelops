# pyexcelops
A lightweight pandas-to-Excel utility that writes multiple DataFrames into a single, presentation-ready .xlsx workbook: styled headers, auto-fitted columns, wrapped text, and native Excel tables.
